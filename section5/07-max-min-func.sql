Select MAX(num_guests) from bookings;

Select MIN(num_guests) from bookings;

Select MAX(amount_billed) AS max_billed, MAX(amount_tipped) AS max_tipped from bookings;

Select MAX(category), MIN(category) from tables;

Select MAX(booking_date), MIN(booking_date) from bookings;